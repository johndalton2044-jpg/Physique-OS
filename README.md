# Physique-OS
An offline-first, single-file adaptive body-composition operating system. It separates what was measured from what was derived, assumed, estimated or predicted; it changes one variable at a time with a prediction stamped before the outcome; and it keeps what did not work. It interprets a training and nutrition record; it does not diagnose disease.
